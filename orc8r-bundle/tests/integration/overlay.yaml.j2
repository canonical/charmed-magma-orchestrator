applications:
  fluentd:
    options:
      domain: {{ domain }}
  orc8r-certifier:
    options:
      domain: {{ domain }}
  orc8r-nginx:
    options:
      domain: {{ domain }}
  tls-certificates-operator:
    options:
      generate-self-signed-certificates: true
      ca-common-name: rootca.{{ domain }}
