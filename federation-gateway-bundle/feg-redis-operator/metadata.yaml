# Copyright 2022 Canonical Ltd.
# See LICENSE file for licensing details.

name: magma-feg-redis
description: |
  Federation Gateway redis service.
summary: |
  Federation Gateway redis service.

containers:
  magma-feg-redis:
    resource: magma-feg-redis-image
    mounts:
      - storage: config
        location: /var/opt/magma/tmp

storage:
  config:
    type: filesystem
    description: Service config storage
    minimum-size: 1M

resources:
  magma-feg-redis-image:
    type: oci-image
    description: OCI image for magma-feg-redis (docker.artifactory.magmacore.org:443/artifactory/feg-prod/gateway_python:1.6.1)
    upstream-source: docker.artifactory.magmacore.org:443/artifactory/feg-prod/gateway_python:1.6.1
