name: feg-session-proxy

on:
  push:
    paths:
      - "federation-gateway-bundle/feg-session-proxy-operator/**"

jobs:
  feg-session-proxy-lint-report:
    name: Lint report (feg-session-proxy)
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Install tox
        run: pip install tox
      - name: Run tests using tox
        run: cd federation-gateway-bundle/feg-session-proxy-operator && tox -e lint

  feg-session-proxy-static-analysis:
    name: Static analysis (feg-session-proxy)
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Install tox
        run: pip install tox
      - name: Run tests using tox
        run: cd federation-gateway-bundle/feg-session-proxy-operator && tox -e static

  feg-session-proxy-unit-tests-with-coverage:
    name: Unit tests (feg-session-proxy)
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Install tox
        run: pip install tox
      - name: Run tests using tox
        run: cd federation-gateway-bundle/feg-session-proxy-operator && tox -e unit

  feg-session-proxy-integration-test:
    name: Integration tests (feg-session-proxy)
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup operator environment
        uses: charmed-kubernetes/actions-operator@main
        with:
          provider: microk8s
      - name: Run integration tests
        run: cd federation-gateway-bundle/feg-session-proxy-operator && tox -e integration
