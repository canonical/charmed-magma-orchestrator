name: feg-aaa-server

on:
  push:
    paths:
      - "federation-gateway-bundle/feg-aaa-server-operator/**"

jobs:
  feg-aaa-server-lint-report:
    name: Lint report (feg-aaa-server)
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Install tox
        run: pip install tox
      - name: Run tests using tox
        run: cd federation-gateway-bundle/feg-aaa-server-operator && tox -e lint

  feg-aaa-server-static-analysis:
    name: Static analysis (feg-aaa-server)
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Install tox
        run: pip install tox
      - name: Run tests using tox
        run: cd federation-gateway-bundle/feg-aaa-server-operator && tox -e static

  feg-aaa-server-unit-tests-with-coverage:
    name: Unit tests (feg-aaa-server)
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Install tox
        run: pip install tox
      - name: Run tests using tox
        run: cd federation-gateway-bundle/feg-aaa-server-operator && tox -e unit

  feg-aaa-server-integration-test:
    name: Integration tests (feg-aaa-server)
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup operator environment
        uses: charmed-kubernetes/actions-operator@main
        with:
          provider: microk8s
      - name: Run integration tests
        run: cd federation-gateway-bundle/feg-aaa-server-operator && tox -e integration
