name: feg-swx-proxy

on:
  push:
    paths:
      - "federation-gateway-bundle/feg-swx-proxy-operator/**"

jobs:
  feg-swx-proxy-lint-report:
    name: Lint report (feg-swx-proxy)
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Install tox
        run: pip install tox
      - name: Run tests using tox
        run: cd federation-gateway-bundle/feg-swx-proxy-operator && tox -e lint

  feg-swx-proxy-static-analysis:
    name: Static analysis (feg-swx-proxy)
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Install tox
        run: pip install tox
      - name: Run tests using tox
        run: cd federation-gateway-bundle/feg-swx-proxy-operator && tox -e static

  feg-swx-proxy-unit-tests-with-coverage:
    name: Unit tests (feg-swx-proxy)
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Install tox
        run: pip install tox
      - name: Run tests using tox
        run: cd federation-gateway-bundle/feg-swx-proxy-operator && tox -e unit

  feg-swx-proxy-integration-test:
    name: Integration tests (feg-swx-proxy)
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup operator environment
        uses: charmed-kubernetes/actions-operator@main
        with:
          provider: microk8s
      - name: Run integration tests
        run: cd federation-gateway-bundle/feg-swx-proxy-operator && tox -e integration
